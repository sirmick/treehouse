# Vagrant
.vagrant/
*.box

# Ansible
*.retry
# generated by bin/up.sh — per-host VM IP, not portable
ansible/inventory/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Node / SvelteKit
node_modules/
launcher/.svelte-kit/

# Editor
.idea/
.vscode/
*.swp
*~

# Claude Code's per-project runtime state (scheduled-task locks, etc.)
.claude/

# Local secrets and state — never commit
config/secrets.yml
state/
logs/
content/
launcher/build/

# Per-deployment settings (LAN IPs, public domain). The template lives
# at treehouse.example.yml; copy and edit locally.
treehouse.yml

# Restic password files
*.restic-password

# OS
.DS_Store
