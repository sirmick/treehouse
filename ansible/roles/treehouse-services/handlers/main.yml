---
- name: docker compose up
  # --remove-orphans cleans up containers that were in a previous
  # compose.yml but aren't now (e.g. when we dropped the proxy
  # service). Without it, the stale containers linger.
  ansible.builtin.command:
    cmd: docker compose -f {{ treehouse_root }}/compose.yml up -d --remove-orphans
